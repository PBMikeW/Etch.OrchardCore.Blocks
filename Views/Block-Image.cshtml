@{
    var tag = new TagBuilder("figure");

    if ((bool?)Model.Stretched == true)
    {
        tag.AddCssClass("align--wide");
    }

    tag.AddCssClass("figure");
}

@tag.RenderStartTag()
    <img src="@Model.Url" alt="@Model.Caption" />

    @if (Model.HasCaption)
    {
        <figcaption>@Html.Raw(Model.Caption)</figcaption>
    }
@tag.RenderEndTag()